<!DOCTYPE html>
<html lang="he" dir="rtl">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>הגדרת מנהל ראשון - מערכת ניהול תורים</title>
  <link rel="stylesheet" href="/css/style.css">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
</head>
<body>
  <div class="container">
    <div class="auth-container">
      <div class="auth-card">
        <div class="auth-header">
          <i class="fas fa-user-shield auth-icon"></i>
          <h1>הגדרת מנהל ראשון</h1>
          <p>צור את המשתמש המנהל הראשון למערכת</p>
        </div>

        <% if (errorMessage) { %>
          <div class="alert alert-error">
            <i class="fas fa-exclamation-triangle"></i>
            <%= errorMessage %>
          </div>
        <% } %>

        <form action="/admin/setup" method="POST" class="auth-form">
          <div class="form-group">
            <label for="username">
              <i class="fas fa-user"></i>
              שם משתמש
            </label>
            <input 
              type="text" 
              id="username" 
              name="username" 
              required 
              placeholder="הכנס שם משתמש"
              class="form-input"
            >
          </div>

          <div class="form-group">
            <label for="password">
              <i class="fas fa-lock"></i>
              סיסמה
            </label>
            <input 
              type="password" 
              id="password" 
              name="password" 
              required 
              placeholder="הכנס סיסמה"
              class="form-input"
            >
          </div>

          <button type="submit" class="btn btn-primary btn-full">
            <i class="fas fa-user-plus"></i>
            צור מנהל
          </button>
        </form>

        <div class="auth-footer">
          <p>לאחר יצירת המנהל, תוכל להתחבר למערכת</p>
        </div>
      </div>
    </div>
  </div>

  <script>
    // הוספת אנימציות
    document.addEventListener('DOMContentLoaded', function() {
      const form = document.querySelector('.auth-form');
      const inputs = document.querySelectorAll('.form-input');
      
      // אנימציה לטופס
      form.style.opacity = '0';
      form.style.transform = 'translateY(20px)';
      
      setTimeout(() => {
        form.style.transition = 'all 0.5s ease';
        form.style.opacity = '1';
        form.style.transform = 'translateY(0)';
      }, 100);

      // אנימציה לשדות הקלט
      inputs.forEach((input, index) => {
        input.style.opacity = '0';
        input.style.transform = 'translateX(20px)';
        
        setTimeout(() => {
          input.style.transition = 'all 0.3s ease';
          input.style.opacity = '1';
          input.style.transform = 'translateX(0)';
        }, 200 + (index * 100));
      });
    });
  </script>
</body>
</html>
